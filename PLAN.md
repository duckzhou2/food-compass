# 食物罗盘 MVP 实施计划

## 成功标准

- 从本地、可追溯数据生成可运行的奶茶罗盘。
- 八品牌、分类、默认热量区间和关键词组合筛选正确，三组条件均支持多选。
- 产品在列表与转盘中只出现一次，动画与结果共用一次随机选择。
- 规格选项只展示真实存在的后续组合，小料按原表单位累计。
- `null` 自然显示为暂无数据，不插值、不平均、不产生 NaN。
- 首页、罗盘、数据说明和关于页在手机与桌面均可使用。
- lint、typecheck、单元/组件测试和生产构建全部通过。

## 步骤与验证

1. 数据审计与派生层 → `python scripts/import-milk-tea-data.py`
2. 页面与数据访问层 → `pnpm typecheck`
3. 筛选、随机和结果展示 → `pnpm test`
4. 视觉规范与响应式升级 → Playwright 375 / 768 / 1440 px 两轮截图
5. 生产交付 → `pnpm lint && pnpm build`

## 范围约束

- 原始数据位于父目录，保持只读。
- MVP 使用静态 JSON，不连接 PostgreSQL。
- 不实现价格筛选，不抓取产品图，不提供健康建议。
- Excel 中的内部备注、冲突与异常进入导入报告，不在普通页面展示。
