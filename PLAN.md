# 食物罗盘 MVP 实施计划

## 成功标准

- 从本地、可追溯数据生成可运行的奶茶罗盘。
- 品牌、品类、热量、审计状态、在售状态和关键词组合筛选正确。
- 转盘从完整候选池等概率抽取，动画与结果共用一次随机选择。
- `null` 自然显示为“待核验”，来源性质不被升级或伪装。
- 首页、罗盘、数据说明和关于页在手机与桌面均可使用。
- lint、typecheck、单元/组件测试和生产构建全部通过。

## 步骤与验证

1. 数据审计与派生层 → `pnpm data:validate`
2. 页面与数据访问层 → `pnpm typecheck`
3. 筛选、随机和结果展示 → `pnpm test`
4. 视觉规范与响应式升级 → Playwright 375 / 768 / 1440 px 两轮截图
5. 生产交付 → `pnpm lint && pnpm build`

## 范围约束

- 原始数据位于父目录，保持只读。
- MVP 使用静态 JSON，不连接 PostgreSQL。
- 不实现价格筛选，不抓取产品图，不提供健康建议。
- 网络补充数据作为独立 variant，保留来源、历史口径与可信度。
